1 - Open terminal window in base folder and run: 
    docker-compose build
2 - Run kafka producer with 
    docker-compose run -it kafkaproducer
3 - Run kafka consumer with 
    docker-compose run -it kafkaconsumer